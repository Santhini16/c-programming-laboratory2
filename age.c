#include <stdio.h>
int main()
{
   int age;
   scanf("%d",&age);
   if(age>=18)
   {
   printf("the person is eligible for voting" );
   }
   else
   {
       printf("the person is not eligible for voting");
   }
}
