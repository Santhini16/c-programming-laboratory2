int main()
{
    int year;
    scanf("%d",&year);
    if(year%4==0)
    {
        printf("the yaer %d is a leap year",year);
    }
    else
    {
        printf("the year %d is not a leap year",year);
    }
}
