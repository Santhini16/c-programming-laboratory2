#include <stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a<b)
    {
        printf("the number %d is smallest ",a);
    }
    else
    {
        printf("the number %d is smallest",b);
}
}
